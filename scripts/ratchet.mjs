// Stub until the conformance apparatus bead vendors the corpus and the
// evaluator registry exists to count. Not a gate stage, so it fails loudly
// rather than reporting a count it cannot compute.
console.error("ratchet: not yet");
process.exit(1);
