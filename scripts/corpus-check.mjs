// Stub until the conformance apparatus bead vendors the corpus.
//
// This one exits 0, unlike its two siblings: `corpus:check` is a stage of the
// full gate, and a stage that always fails would make the gate unrunnable
// from the moment this scaffold lands. "No corpus vendored yet" is a true
// statement about this tree and not a check that was skipped for convenience;
// the apparatus bead replaces this file with the real check.
console.log("corpus:check: no corpus vendored yet");
process.exit(0);
