// Stub until the conformance apparatus bead vendors the corpus. Not a gate
// stage, so it fails loudly rather than pretending to have refreshed
// anything.
console.error("corpus:refresh: not yet");
process.exit(1);
